package me.RockinChaos.itemjoin.utils;

import org.bukkit.command.CommandSender;

public class ItemCreator {
	
	// This is a currently unimplemented feature that is currently in development so it is blocked so only the DEV can work on it.
	// This will soon be the items GUI creator that allows you to create items in game for ItemJoin!
	public static void LaunchCreator(CommandSender sender) {
		//ServerHandler.sendConsoleMessage("" + player.getUniqueId());
		//String PROFILE_URL = "https://sessionserver.mojang.com/session/minecraft/profile/";
		//JSONParser jsonParser = new JSONParser();
      //  JSONObject response;
      //  String name = null;
		//try {
	   //     HttpURLConnection connection = (HttpURLConnection) new URL(PROFILE_URL + player.getUniqueId().toString().replace("-", "")).openConnection();
		//	response = (JSONObject) jsonParser.parse(new InputStreamReader(connection.getInputStream()));
		//	name = (String) response.get("name");
		//} catch (IOException | ParseException e) {
		//	// TODO Auto-generated catch block
		//	e.printStackTrace();
		//}
		//ServerHandler.sendConsoleMessage("" + name);
	}

}
